<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.club.club.mapper.ClubApplyMapper">
    <!-- 分页查询创始人的待审批列表 -->
    <select id="selectPendingByFounderId" resultType="com.club.club.entity.ClubApply">
        SELECT * FROM club_apply
        WHERE founderId = #{founderId} AND status = 0
        ORDER BY applyTime DESC
    </select>

    <!-- 查询用户是否已提交过该社团的申请 -->
    <select id="selectByClubIdAndApplicantId" resultType="com.club.club.entity.ClubApply">
        SELECT * FROM club_apply
        WHERE clubId = #{clubId} AND applicantId = #{applicantId}
          AND status IN (0, 1) -- 待审批或已同意的申请视为重复
    </select>
</mapper>